import 'package:flutter/material.dart';

class Kbody extends StatelessWidget {
  const Kbody({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset("images/01 Online Shopping 2.png"),
    );
  }
}
